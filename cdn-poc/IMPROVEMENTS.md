# Improvements

- Add stale-while-revalidate and background re-fetch behavior.
- Model compression variants (`br`, `gzip`) and cache-key normalization.
- Add shield layers or parent caches between edge and origin.
- Track invalidation propagation delay instead of making purge instantaneous.
- Add popularity analytics and proactive edge pre-warming.
- Support binary assets and size-aware eviction instead of object-count capacity only.
