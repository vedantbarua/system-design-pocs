# Improvements

- Add binary upload support with multipart checksum validation.
- Introduce bucket quotas, per-prefix metrics, and request rate charts.
- Model lifecycle policies that transition objects between storage classes over time.
- Add delete markers and explicit restore flows for archived objects.
- Persist metadata and blobs separately to simulate object-store internals more accurately.
- Add tenant isolation and bucket policy enforcement for signed-request authorization.
